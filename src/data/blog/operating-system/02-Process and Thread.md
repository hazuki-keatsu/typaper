---
title: Operating System - 02 - Process and Thread
author: Hazuki Keatsu
pubDatetime: 2026-05-18T14:31:21.353Z
protocol: CC BY-NC
featured: false
draft: false
tags:
  - OS
  - Note
description: 操作系统课程学习记录
---

## 2.1 进程（Processes）

### 进程模型
- **进程**：正在执行的程序，包括程序计数器、寄存器、变量等当前值。

- **多道程序**：内存中同时存在多个程序，但任意时刻只有一个程序在CPU上执行。

- **进程组成**：
  - **代码段**：存放程序的可执行机器指令，通常是只读的，避免进程意外修改自身指令，同时多个进程共享同一段代码。
  
  - **数据段**：用于存放全局变量和静态变量，通常分为已初始化数据段（.data，存放已初始化的全局变量和静态变量）和未初始化数据段（.bss，存放未初始化的全局变量和静态变量，程序加载时，系统会将这片区域清零）
  
  - **堆**：用于动态内存分配，地址从低向高增长（通常高于数据段）；由程序员手动管理，若忘记释放则可能导致**内存泄漏**。
  
  - **栈**：存放函数调用信息，包括函数的参数、局部变量、函数返回地址和保存的寄存器状态（上下文）。特点是地址从高向低增长；由编译器自动分配和释放；每个线程有自己独立的栈。
  
  - **进程控制块（PCB, Process Control Block）**：不是内存中的代码或数据，而是操作系统内核为每个进程维护的一个**数据结构**，记录进程的运行状态和资源信息。
    - **进程标识符**（PID）
    - **进程状态**（就绪、运行、阻塞等）
    - **程序计数器**（PC，下一条指令地址）
    - **CPU寄存器**（上下文切换时保存）
    - **内存管理信息**（页表基址、段界限等）
    - **打开文件表**（已打开的文件描述符）
    - **信号掩码/处理信息**
    - **优先级、记账信息**等
    
    ![image-20260517105305402](/images/operating-system/image-20260517105305402.png)


### 进程创建
- **创建事件**：
  - 系统初始化
  - 系统调用（如 `fork`）
  - 用户请求创建新进程
  - 批处理作业初始化
- **进程树**：父进程创建子进程，子进程可再创建后代，形成树状结构。
- **UNIX 示例**：
  - `fork()`：创建子进程，复制父进程地址空间
  - `execve()`：在子进程中加载新程序

### 进程终止
- **终止条件**：
  - 正常退出（`Normal exit (Voluntary)`）
  - 错误退出（`Error exit (Voluntary)`）
  - 致命错误（`Fatal erro (Involuntary)`）
  - 被其他进程杀死（`(Involuntary)`）
- **级联终止**：父进程终止时，操作系统可能自动终止其所有子进程。

### 进程状态
- **五状态模型**：
  - **新建（new）**：正在创建
  - **就绪（ready）**：等待分配CPU
  - **运行（running）**：正在执行
  - **阻塞（waiting/blocked）**：等待某个事件（如I/O）
  - **终止（terminated）**：执行结束

<img src="/images/operating-system/image-20260517104335334.png" alt="image-20260517104335334" style="zoom: 50%;" />

### 进程间切换

- 时钟中断
- I/O中断
- 由用户程序触发的操作系统用户态与内核态的切换（Trap）
- 内存缺页
- 管理程序的调用（Supervisor call）

<img src="/images/operating-system/image-20260517104800384.png" alt="image-20260517104800384" style="zoom:50%;" />

### 上下文切换

- **定义**：CPU从一个进程切换到另一个进程时，保存当前进程的状态（PCB），恢复下一个进程的状态。
- **开销**：纯额外负担，不执行有用工作。

### 进程调度队列
- **作业队列**：系统中所有进程
- **就绪队列**：内存中准备好运行的进程
- **设备队列**：等待某个I/O设备的进程

---

## 2.2 线程（Threads）

### 线程模型
- **线程**：轻量级进程，是CPU调度的基本单位。
- **共享资源**：同一进程内的线程共享代码段、数据段、操作系统资源（如打开的文件）。
- **私有资源**：每个线程拥有自己的程序计数器、寄存器集、栈。
- **优点**：
  - 创建和销毁开销小
  - 线程间切换比进程间切换快
  - 适合并行计算和I/O重叠操作
  - 在多CPU系统中实现真正并行

<img src="/images/operating-system/image-20260517105649497.png" alt="image-20260517105649497" style="zoom:50%;" />

### 线程实现方式

| 方式                  | 特点                           | 优点                                    | 缺点                           | 图片                                                         |
| --------------------- | ------------------------------ | --------------------------------------- | ------------------------------ | ------------------------------------------------------------ |
| **用户级线程（ULT）** | 内核不感知，由用户空间库管理   | 切换快，无需内核干预，可定制调度        | 一个线程阻塞会导致整个进程阻塞 | <img src="/images/operating-system/image-20260517110426519.png" alt="image-20260517110426519" style="zoom: 67%;" /> |
| **内核级线程（KLT）** | 内核管理所有线程               | 线程阻塞不影响同进程其他线程，适合多CPU | 切换开销大，需要内核表项       | <img src="/images/operating-system/image-20260517110442471.png" alt="image-20260517110442471" style="zoom: 67%;" /> |
| **混合实现**          | 用户级线程多路复用到内核级线程 | 兼顾两者优点                            | 实现复杂                       | <img src="/images/operating-system/image-20260517110748161-1778987274196-12.png" alt="image-20260517110748161" style="zoom: 67%;" /> |

### 其他线程概念
- **弹出式线程**：消息到达时动态创建，无历史状态，响应快。

  ![image-20260517110928305](/images/operating-system/image-20260517110928305.png)
- **线程表**：用户级线程在进程内维护，内核级线程在内核中维护。

---

## 2.3 调度（Scheduling）

### 何时触发调度

- 一个新的进程创建
- 一个进程退出
- 一个被I/O或者信号量阻断

### 调度算法的目标

- 所有系统通用目标
  - **公平性（Fairness）**：为每个进程分配公平的CPU时间份额
  - **策略执行（Policy enforcement）**：确保既定的调度策略能够被落实
  - **平衡性（Balance）**：让系统的所有部分都保持工作状态
- 批处理系统（Batch systems）
  - **吞吐量（Throughput）**：最大化每小时完成的作业数量
  - **周转时间（Turnaround time）**：最小化作业从提交到完成的时间间隔
  - **CPU利用率（CPU utilization）**：让CPU始终处于忙碌状态
- 交互式系统（Interactive systems）
  - **响应时间（Response time）**：快速响应用户请求
  - **比例性（Proportionality）**：满足用户对响应速度的预期
- 实时系统（Real-time systems）
  - **满足截止时间（Meeting deadlines）**：避免因超时导致数据丢失
  - **可预测性（Predictability）**：避免多媒体系统中出现的质量下降问题

### 常见调度算法详解

#### 在批处理系统中的调度算法

| 算法                  | 描述                                     | 优缺点                               |
| --------------------- | ---------------------------------------- | ------------------------------------ |
| **FCFS (First-come first-served)**             | 先来先服务                               | 简单，但是CPU利用率较低              |
| **SJF (Shortest job first)**    | 执行最短CPU突发的进程                    | 最小平均等待时间，但需要预知突发时间 |
| **SRTF (Shortest remaining time first)** | 剩余时间最短的优先                       | 最优平均等待时间                     |

#### 在交互系统中的调度算法

| 算法 | 描述 | 优缺点 |
| :--- | :--- | :--- |
| **RR (Round Robin)** | 循环轮转 | 如果时间片设置得比平均CPU突发时间长，抢占将很少发生。但频繁的上下文切换会消耗较多时间。 |
| **Priority scheduling** | 优先调度 | 每个进程被分配一个优先级，优先级高的先执行。但如果所有进程优先级都最高，算法会退化为先来先服务；低优先级进程可能产生饥饿。 |
| **Shortest process next** | 最短作业优先 | 总是产生最小的平均周转时间。问题在于如何确定当前可运行进程中最短的那个，一种方法是根据过去的行为进行估算，并运行估计运行时间最短的进程。 |
| **Multiple queues** | 多级队列 | 将进程按类型（如前台/后台）分配到不同队列，每个队列有自己的调度算法。队列间通常采用固定优先级抢占。**优点**：隔离不同类型任务，灵活配置。**缺点**：队列间可能导致低优先级队列饥饿；配置复杂。 |
| **Guaranteed scheduling** | 保证调度 | 系统承诺每个进程获得一定比例的CPU时间（如1/n）。通过跟踪每个进程实际获得的CPU时间与应获得时间的差值，调度时优先选择差距最大的进程。**优点**：提供绝对公平性，避免进程饥饿。**缺点**：需要实时记账和计算，增加系统开销。 |
| **Lottery scheduling** | 彩票调度 | 每个进程分配若干“彩票”，每次调度时随机抽取一张彩票，持有该彩票的进程获得CPU。优先级可通过分配彩票数量来调节。**优点**：实现简单，可避免饥饿，响应性好。**缺点**：随机性导致短期不公平；需要高质量的随机数生成。 |
| **Fair-share scheduling** | 公平分享调度 | 以用户或组为单位分配CPU份额，而不是以进程为单位。即使某个用户创建了大量进程，也只能获得其用户层级所分配的份额。**优点**：保证用户间的资源公平，防止单一用户过度占用。**缺点**：实现复杂，需要跟踪每个用户的资源使用情况，且可能降低单个进程的响应速度。 |

#### 在实时系统中的调度算法

给定 $m$ 个周期事件，事件 $i$ 在周期 $P_i$ 内发生，并且每个事件需要 $C_i$ 秒的 CPU 时间来处理
那么只有当负载满足以下条件时才能处理：

$$\sum\limits_{i=1}^{m}\dfrac{C_i}{P_i} \leq 1$$

符合此条件的实时系统称为**可调度的**。

#### 线程调度

<div style="display: flex;"><img src="/images/operating-system/image-20260517125650969.png" alt="image-20260517125650969" style="zoom:50%;" align="left"/><img src="/images/operating-system/image-20260517125610584.png" alt="image-20260517125610584" style="zoom:50%;" align="left" /></div>

### 调度时机

- 进程从运行→等待（I/O、信号量等）
- 进程从运行→就绪（时间片用完）
- 进程从等待→就绪（I/O完成）
- 进程终止

### 调度与分派器
- **分派器（Dispatcher）**：负责上下文切换、切换到用户态、跳转到用户程序适当位置。
- **分派延迟**：停止一个进程并启动另一个所需时间。

### 调度评判标准

- CPU 利用率——让 CPU 尽可能忙碌（Max）
- 吞吐量——每时间单元完成执行的进程数量（Max）
- 周转时间——从提交到完成的间隔（Min）
- 等待时间——进程在准备队列中等待的时间（Min）
- 响应时间——从请求提交到首次响应产生所需时间，而非输出（分时环境下）（Min）

### 各类调度算法详解

1. 先来先服务（FCFS）

   - 按到达顺序调度，非抢占。
   - 缺点是可能导致**护送效应**（短进程被长进程阻塞）。
2. 短作业优先（SJF）

   - 选择下一个CPU执行时间最短的进程。
   - **两种模式**：
     - **非抢占式**：一旦获得CPU，运行至完成。
     - **抢占式**（最短剩余时间优先，SRTF）：新进程的剩余时间更短时可抢占当前进程。
   - **最优性**：SJF在平均等待时间上理论最优。
3. 优先级调度
   - 每个进程有优先级（整数），优先级最高的先运行。
   - 可以是抢占式或非抢占式。
   - SJF是优先级调度的一种特例。
   - **问题**：低优先级进程可能**饥饿**。
   - **解决方案**是**老化**（Aging）随时间推移提高进程优先级。
4. 时间片轮转（Round Robin，RR）
   - 每个进程运行一个固定的**时间片**（通常10-100ms），然后放回就绪队列末尾。
   - 时间片过长 → 退化为FCFS；过短 → 上下文切换开销过大。
5. 多级队列调度
   - 就绪队列划分为多个独立队列。
6. 多级反馈队列
   - 进程可在不同队列之间移动，支持**老化**。
7. 传统UNIX调度

   传统UNIX调度器采用**多级反馈队列 + 时间片轮转**的设计，核心思想是：**CPU密集型进程获得低优先级、长时间片，I/O密集型进程获得高优先级、短时间片**，从而在公平性和响应性之间取得平衡。

   **优先级计算**

   优先级每秒重新计算一次（在时钟中断处理中触发），公式如下：

   $$cpu[j](i)=\dfrac{cpu[j](i-1)}{2}$$

   $$p[j](i)=Base[j]+\dfrac{cpu[j](i)}{2}+nice[j]$$

   其中各符号含义：

   | 符号 | 含义 |
   | :--- | :--- |
   | $cpu[j](i)$ | 进程 $j$ 在第 $i$ 个间隔的**CPU使用量**（以时钟滴答为单位） |
   | $Base[j]$ | 进程 $j$ 的**基准优先级**，由进程类型决定（内核态进程的 Base 值小于用户态进程，即内核态优先级更高） |
   | $nice[j]$ | 用户设置的**nice值**，范围通常为 $-20$ 到 $+19$（默认 $0$），数值越大表示进程越"谦让"，优先级越低 |
   | $p[j](i)$ | 进程 $j$ 在第 $i$ 个间隔计算出的**优先级数值** |

   **注意**：在传统UNIX中，**优先级数值越小，实际优先级越高**。

   **公式解读**

   第一条公式 $cpu[j](i)=\dfrac{cpu[j](i-1)}{2}$ 是一个**指数衰减**过程：
   - 每个间隔，进程的历史CPU使用量被折半，这意味着**越久远的CPU使用对当前优先级的影响越小**。
   - 如果一个进程之前占用大量CPU但在最近几个间隔里没有运行，它的 $cpu$ 值会迅速衰减趋近于零，相当于一个自动的**老化（Aging）机制**，防止CPU密集型进程永远被饿死。

   第二条公式 $p[j](i)=Base[j]+\dfrac{cpu[j](i)}{2}+nice[j]$ 决定了进程的最终调度优先级：
   - $Base[j]$ 提供按进程类型区分的基准——例如，等待终端输入的任务应该比后台批处理任务更灵敏。
   - $+\dfrac{cpu[j](i)}{2}$ 是**惩罚项**：进程近期CPU使用越多，这个值越大，优先级数值越大（即实际优先级越低），自然让出CPU给其他进程。
   - $+nice[j]$ 允许用户**手动降低**进程优先级（数值越大，实际优先级越低），只有root用户可以设置负的nice值来提升优先级。

   **优先级区间与调度策略**

   计算出的优先级数值被划分到多个固定区间（优先级带），每个区间对应不同的调度行为：

   | 优先级区间（数值） | 实际优先级 | 调度策略 |
   | :---: | :---: | :--- |
   | 小（如 0–49） | 高 | 分配**短时间片**（通常约 100ms），适合交互式/I/O密集型进程，保证响应速度 |
   | 大（如 50+） | 低 | 分配**长时间片**，适合CPU密集型后台计算，减少不必要的上下文切换 |

   这种设计自然实现了**对I/O密集型进程的奖励**：一个进程频繁等待I/O时不会消耗CPU，$cpu$ 值自动衰减，优先级提升，当I/O就绪后能迅速获得CPU响应；相反，一个持续占用CPU的计算进程 $cpu$ 值不断累加，优先级逐步降低，但在它终于被切换出去后，$cpu$ 值又会在等待期间衰减，最终重新获得运行机会。

   **整体流程**

   1. 每秒时钟中断触发时，内核遍历所有就绪进程，按上述公式重新计算每个进程的 $cpu[j]$ 和 $p[j]$。
   2. 根据 $p[j]$ 将进程映射到对应的优先级区间，分配相应的时间片。
   3. 在同一优先级区间内，采用**时间片轮转（RR）**：每个进程用完时间片后被放到队尾，等待下一轮。
   4. 当有更高优先级区间的进程就绪时，可以**抢占**当前正在运行的低优先级进程。

   这种调度机制在当时的硬件条件下非常高效，但随着多核CPU和大规模并发的发展，后来被更复杂的调度器（如Linux的CFS，Completely Fair Scheduler）所取代。理解传统UNIX调度的设计思想，是学习现代操作系统调度器演进的基石。
